#!/bin/bash

echo "🧹 Limpiando cache de Metro..."
npx expo start --clear

echo "✅ Cache limpiado. La app debería reiniciarse automáticamente."
echo "📱 Si las fuentes no aparecen, reinicia manualmente la app en tu dispositivo."
